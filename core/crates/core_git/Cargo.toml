[package]
name = "core_git"
version = "0.1.0"
edition = "2021"

[dependencies]
core_types = { path = "../core_types" }

[dev-dependencies]
insta = { version = "1", features = ["yaml"] }
serde_json = "1"
tempfile = "3"
