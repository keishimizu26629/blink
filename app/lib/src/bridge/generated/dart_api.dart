// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.11.1.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import 'frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These functions are ignored because they are not marked as `pub`: `map_err`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `from`, `from`, `from`, `from`, `from`, `from`

/// プロジェクトを開く
Future<String> openProject({required String rootPath}) =>
    RustLib.instance.api.crateDartApiOpenProject(rootPath: rootPath);

/// ディレクトリ内のファイル一覧を返す
Future<List<DartFileNode>> listDir({
  required String rootPath,
  required String dirPath,
}) => RustLib.instance.api.crateDartApiListDir(
  rootPath: rootPath,
  dirPath: dirPath,
);

/// ファイルの内容を文字列として読み込む
Future<String> readFile({required String path}) =>
    RustLib.instance.api.crateDartApiReadFile(path: path);

/// シンタックスハイライト
Future<List<DartTokenSpan>> highlightRange({
  required String path,
  required int startLine,
  required int endLine,
}) => RustLib.instance.api.crateDartApiHighlightRange(
  path: path,
  startLine: startLine,
  endLine: endLine,
);

/// Git Blame
Future<List<DartBlameLine>> blameRange({
  required String path,
  required int startLine,
  required int endLine,
}) => RustLib.instance.api.crateDartApiBlameRange(
  path: path,
  startLine: startLine,
  endLine: endLine,
);

/// Blame コミット差分
Future<DartGitFileDiff> blameCommitDiff({
  required String path,
  required String commit,
}) => RustLib.instance.api.crateDartApiBlameCommitDiff(
  path: path,
  commit: commit,
);

/// ファイル差分
Future<DartGitFileDiff> gitFileDiff({required String path}) =>
    RustLib.instance.api.crateDartApiGitFileDiff(path: path);

/// Git status
Future<DartGitStatus> gitStatus({required String rootPath}) =>
    RustLib.instance.api.crateDartApiGitStatus(rootPath: rootPath);

/// 現在のブランチ名
Future<String> gitCurrentBranch({required String rootPath}) =>
    RustLib.instance.api.crateDartApiGitCurrentBranch(rootPath: rootPath);

/// Git Blame 行情報
class DartBlameLine {
  final int line;
  final String author;
  final PlatformInt64 authorTime;
  final String summary;
  final String commit;

  const DartBlameLine({
    required this.line,
    required this.author,
    required this.authorTime,
    required this.summary,
    required this.commit,
  });

  @override
  int get hashCode =>
      line.hashCode ^
      author.hashCode ^
      authorTime.hashCode ^
      summary.hashCode ^
      commit.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DartBlameLine &&
          runtimeType == other.runtimeType &&
          line == other.line &&
          author == other.author &&
          authorTime == other.authorTime &&
          summary == other.summary &&
          commit == other.commit;
}

/// ファイルノード
class DartFileNode {
  final String id;
  final String path;
  final String name;
  final bool isDir;

  const DartFileNode({
    required this.id,
    required this.path,
    required this.name,
    required this.isDir,
  });

  @override
  int get hashCode =>
      id.hashCode ^ path.hashCode ^ name.hashCode ^ isDir.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DartFileNode &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          path == other.path &&
          name == other.name &&
          isDir == other.isDir;
}

/// Git 差分
class DartGitFileDiff {
  final String commit;
  final String path;
  final String diffText;

  const DartGitFileDiff({
    required this.commit,
    required this.path,
    required this.diffText,
  });

  @override
  int get hashCode => commit.hashCode ^ path.hashCode ^ diffText.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DartGitFileDiff &&
          runtimeType == other.runtimeType &&
          commit == other.commit &&
          path == other.path &&
          diffText == other.diffText;
}

/// Git status 分類結果
class DartGitStatus {
  final List<DartGitStatusEntry> staged;
  final List<DartGitStatusEntry> unstaged;
  final List<DartGitStatusEntry> untracked;

  const DartGitStatus({
    required this.staged,
    required this.unstaged,
    required this.untracked,
  });

  @override
  int get hashCode => staged.hashCode ^ unstaged.hashCode ^ untracked.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DartGitStatus &&
          runtimeType == other.runtimeType &&
          staged == other.staged &&
          unstaged == other.unstaged &&
          untracked == other.untracked;
}

/// Git status エントリ
class DartGitStatusEntry {
  final String path;
  final String status;

  const DartGitStatusEntry({required this.path, required this.status});

  @override
  int get hashCode => path.hashCode ^ status.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DartGitStatusEntry &&
          runtimeType == other.runtimeType &&
          path == other.path &&
          status == other.status;
}

/// シンタックスハイライト用トークン
class DartTokenSpan {
  final int line;
  final int startCol;
  final int endCol;

  /// "keyword", "string", "comment", "type", "function",
  /// "number", "operator", "punctuation", "variable", "plain"
  final String tokenType;

  const DartTokenSpan({
    required this.line,
    required this.startCol,
    required this.endCol,
    required this.tokenType,
  });

  @override
  int get hashCode =>
      line.hashCode ^ startCol.hashCode ^ endCol.hashCode ^ tokenType.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DartTokenSpan &&
          runtimeType == other.runtimeType &&
          line == other.line &&
          startCol == other.startCol &&
          endCol == other.endCol &&
          tokenType == other.tokenType;
}
